import React from 'react';

const ChatLayout = ({ children }) => {
    return (
        <main>
            <h1>Chat Layout</h1>
            <div>{children}</div>
        </main>
    );
};

export default ChatLayout;
